{
    "bool" : {
        "must" : $must,
        "must_not" : $must_not,
        "should" : $should,
        "minimum_number_should_match" : $minimum_number_should_match,
        "boost" : $boost,
        "disable_coord" : $disable_coord
    }
}